import { ArrivalStatusPipe } from './arrival-status.pipe';

describe('ArrivalStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new ArrivalStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
